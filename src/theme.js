import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#fcdb7c",
      main: "#e2c56f",
      dark: "#cbb163",
    },
    secondary: {
      main: "#fda403",
      dark: "#cb8402",
    },
  },
});

export default theme;
